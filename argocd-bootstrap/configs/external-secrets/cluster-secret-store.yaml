apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-store
spec:
  provider:
    gcpsm:
      projectID: stayrelevantid
      auth:
        workloadIdentity:
          clusterLocation: asia-southeast2
          clusterName: zenith-x-cluster-testing
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets

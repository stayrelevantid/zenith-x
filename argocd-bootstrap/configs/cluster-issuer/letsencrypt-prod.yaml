apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
  labels:
    project: zenith-x
    env: testing
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: admin@masmasdeploy.my.id
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
      - http01:
          ingress:
            ingressClassName: traefik
